// ... (Existing code)
const mcButton = document.getElementById('mc') as HTMLButtonElement;
let memoryValue = 0;
mcButton.addEventListener('click', () => {
    memoryValue = 0;
display.value = "0";
});